# Diligent E-commerce Demo

This repo contains:
- `generate_ecom_data.py` — generates synthetic e-commerce CSV files
- `ingest_to_sqlite.py` — loads CSVs into ecom.db
- `run_query.py` — example SQLite JOIN query

Generated CSVs and the sqlite database are ignored using `.gitignore`.
